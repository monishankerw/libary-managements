spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI:https://github.com/your-org/config-repo}
          search-paths: '{application}'
          clone-on-start: true
          force-pull: true
          default-label: main
